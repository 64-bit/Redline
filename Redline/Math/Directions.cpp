#include "stdafx.h"
#include "Directions.h"

using namespace Redline;
using namespace mathfu;

const vec3 Directions::Forward = vec3(0.0f, 0.0f, 1.0f);
const vec3 Directions::Backward = vec3(0.0f, 0.0f, -1.0f);
const vec3 Directions::Left = vec3(0.0f, 1.0f, 0.0f);
const vec3 Directions::Right = vec3(0.0f, -1.0f, 0.0f);
const vec3 Directions::Up = vec3(0.0f, 1.0f, 0.0f);
const vec3 Directions::Down = vec3(0.0f, -1.0f, 0.0f);